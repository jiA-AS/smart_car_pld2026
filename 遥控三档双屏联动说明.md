# 遥控三档双屏联动包（s1 左拨杆 → FPGA 屏 + ESP 屏同步切换）

PLD2026 赛道三 · 在"赛道三补齐包"基础上叠加
日期：2026-07-29

---

## 1. 功能与评估结论

**需求**：遥控器左拨杆 s1（三档）同时切换 FPGA 的 LCD 屏和 ESP32 的 OLED 屏，
轨迹、视角、调试信息分屏显示；右拨杆 s2（保护/遥控/比赛）不动。

**评估：可行，且改动链很短**，因为三块积木都是现成的：

1. `dbus.data().s1` 早已解析好（1=上 / 3=中 / 2=下），原来只切 OLED 页；
2. ESP→FPGA 的 100Hz 上行帧（原 V1.0，34 字节）扩 1 字节 → **V1.2（35 字节）**，
   把显示模式带进 FPGA，不占新引脚、不动连线；
3. FPGA 端 `disp_mode` 分发总线现成（阈值、OSD 标签、仪表盘都听它的），
   只把"模式源"从 KEY0 换成**遥控优先、KEY0 兜底**（0.5s 无上行帧自动退回按键，
   拔掉 ESP 调台时功能不丢）。

## 2. 档位映射表（两块屏内容互补，不重复）

| s1 档位 | 模式值 | FPGA LCD 屏 | ESP OLED 屏 |
|---|---|---|---|
| 上 | 0 视角 | 双目画面 + 全 HUD（距离/TTC/识别框/姿态速度行） | 接收页 `[VIEW]`：下行指令 mode/L/R + 四轮目标速度 |
| 中 | 1 轨迹 | **轨迹仪表盘突出**：窗外画面压暗一半 + 窗框青色高亮，右上"轨迹"标签 | 姿态页 `[TRK]`：大字 P/R(deg) + 轮速 + 编码器 |
| 下 | 2 调试 | 夜间阈值（弱光放宽）+ 左上 G1/G2 像素计数调试行 + 右上"夜间"标签 | 发送页 `[DBG]`：上行帧 HEX + IMU 原始值 + 链路计数 |

- 遥控器失联：ESP 端强制发模式 0，OLED 回视角页；
- ESP 不在线（0.5s 无好帧）：FPGA 自动退回 KEY0 按键循环（0→1→2），遥控恢复即夺回。

## 3. 协议变更：上行 V1.0 → V1.2（唯一的不兼容点）

```
V1.0（34B）：AA 55 | 01 | ts(2) | enc×16 | gyro×6 | acc×6         | CRC8(31B)
V1.2（35B）：AA 55 | 03 | ts(2) | enc×16 | gyro×6 | acc×6 | mode(1) | CRC8(32B)
```

- 版本字节 0x01→0x03，CRC 覆盖 31→32 字节；
- **ESP 和 FPGA 必须同时更新本包**，只更一侧 → 版本/CRC 全拒 →
  FPGA 收不到任何帧（姿态/里程计/TTC 全停，看门狗退回 KEY0），
  OLED 调试页可看到 TX 计数涨而 FPGA 侧无反应，按此现象排查；
- 下行 V1.1（FPGA→ESP，9 字节）不变，追踪控制链路不受影响。

## 4. 文件清单（共 16 个，磁盘覆盖后 FPGA 侧 Remove/Add 进工程）

### ESP32 端（6 个）→ 仓库 `esp/`
| 本包路径 | 覆盖到 | 说明 |
|---|---|---|
| ESP32/protocol_v1.h | esp/lib/fpga_link/protocol_v1.h | V1.2 帧结构（35B + disp_mode + CRC32B） |
| ESP32/fpga_link.h | esp/lib/fpga_link/fpga_link.h | send() 加 dispMode 参数 |
| ESP32/fpga_link.cpp | esp/lib/fpga_link/fpga_link.cpp | 组帧写 mode 字节 |
| ESP32/fishbot.cpp | esp/src/fishbot.cpp | s1→模式映射（失联强制 0），send(ticks, mode) |
| ESP32/fishbot_display.h | esp/lib/Displays/fishbot_display.h | 注释更新 |
| ESP32/fishbot_display.cpp | esp/lib/Displays/fishbot_display.cpp | 三页重排+档名标签+失联回视角页 |

### FPGA 端（10 个）→ 仓库 `my_car/rtl/`
| 本包路径 | 覆盖到 | 说明 |
|---|---|---|
| FPGA/替换/top_dual_ov5640_lcd.v | my_car/rtl/top_dual_ov5640_lcd.v | mode_switch 接 rc_tick/rc_mode；sensor_link 引出 disp_mode |
| FPGA/替换/mode_switch.v | my_car/rtl/mode_switch.v | **V2**：遥控优先 + 0.5s 看门狗 + KEY0 兜底 |
| FPGA/替换/sensor_link/v1_decode.v | my_car/rtl/sensor_link/v1_decode.v | **V1.2**：32 数据字节 + 解 disp_mode（>2 钳 0） |
| FPGA/替换/sensor_link/sensor_link.v | my_car/rtl/sensor_link/sensor_link.v | 透传 disp_mode |
| FPGA/替换/sensor_link/atan_lut.v | my_car/rtl/sensor_link/atan_lut.v | **ROM 化**（治综合崩溃，数据与原 case 版逐项一致） |
| FPGA/替换/sensor_link/sincos_lut.v | my_car/rtl/sensor_link/sincos_lut.v | 同上 |
| FPGA/替换/lcd_rgb_top/lcd_rgb_top.v | my_car/rtl/lcd_rgb_top/lcd_rgb_top.v | dash_osd 补接 disp_mode |
| FPGA/替换/lcd_rgb_top/osd/osd_overlay.v | my_car/rtl/lcd_rgb_top/osd/osd_overlay.v | 模式1标签"调试"→"轨迹"；G1/G2 调试行移到模式2 |
| FPGA/替换/lcd_rgb_top/osd/dash_osd.v | my_car/rtl/lcd_rgb_top/osd/dash_osd.v | 轨迹档：窗外压暗 + 窗框青色 |
| FPGA/替换/lcd_rgb_top/osd/osd_font.v | my_car/rtl/lcd_rgb_top/osd/osd_font.v | **ROM 化**（107 字符点阵与原版零差异） |

> 注意：`mode_switch.v` 接口变了（多 rc_tick/rc_mode/rc_active），`v1_decode.v`
> 接口变了（多 disp_mode），`dash_osd.v` 接口变了（多 disp_mode）——
> 替换后务必把旧文件从工程 Remove 再 Add 新文件，否则 Vivado 会用缓存旧版。

## 5. 顺带修复：综合崩溃（技术映射 TclStackFree）

上次的综合崩溃根源是三个巨型 case 查找表（osd_font 2080 项、atan_lut 1025 项、
sincos_lut 360 项）被综合器当任意逻辑优化。本包已把它们全部改为
**initial 初始化数组 + 组合读出**（`(* rom_style = "distributed" *)`），
接口、时序、数据与原 case 版完全一致（已逐项比对验证），
综合器会直接推断 ROM 结构，不再生成巨型 MUX 树。

综合设置仍建议保持：
- `Settings → Synthesis → -flatten_hierarchy = none`
- 若仍紧张：`-directive RuntimeOptimized`

## 6. 上板验证清单

1. **编译顺序**：先烧 ESP（V1.2），再综合烧 FPGA——顺序反了也只是暂时收不到帧，不损坏；
2. OLED 三页：s1 上/中/下 → `[VIEW]`/`[TRK]`/`[DBG]` 三页切换；
3. LCD 三档：s1 中档 → 窗外变暗 + 轨迹窗青框 + 右上"轨迹"；s1 下档 → 左上 G1/G2 + 右上"夜间"；
4. 兜底：拔掉 ESP→FPGA 的 TX 线（或关 ESP）0.5s 后，按 KEY0 应能循环切档；插回立刻恢复遥控档；
5. 失联：关遥控器 → OLED 回视角页、FPGA 回模式 0、车停车（原有保护不变）；
6. 回归：比赛模式（s2 下）追踪/TTC 预警/姿态/轨迹功能与升级前一致（下行协议没变）。

## 7. 设计取舍备注

- 模式字节走上行帧而不是新帧型：复用现有 100Hz 心跳，FPGA 看门狗直接用
  parse_done，不用为新帧单独做超时；
- 版本字节升到 0x03 而不是沿用 0x01 偷偷变长：让"只升了一侧"的错误显式暴露
  （全帧被拒），而不是一半数据错位还假装在工作；
- KEY0 保留兜底：比赛现场 ESP 死机/线缆松动时，识别阈值和显示仍可现场调。
